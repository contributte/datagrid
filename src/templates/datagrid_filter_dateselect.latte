{**
 * @param Filter                $filter
 * @param Nette\Forms\Container $input
 * @param string                $icon_prefix       Icon prefix (fa fa-)
 *}
{*php dump($input["date"])*}
{var $container = $input}
{if $outer}
	<div>
		{input $container['date']}
	</div>
{else}
	<div>
		{input $container['date']} {* , class=>"select2 form-control form-control-sm"*}
	</div>
{/if}
